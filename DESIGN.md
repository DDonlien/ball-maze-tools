# 视觉规范

## 适用范围

本仓库主要包含脚本和轻量 Web 工具。UE、Blender 脚本没有自定义 UI，沿用宿主应用界面。

## Web 工具

- `web-maze-builder/` 和 `web-hermite-spline-generator/` 的视觉细节分别记录在各自目录的 `DESIGN.md`。
- 新增 Web UI 时优先保持工具型界面清晰、紧凑，确保主要操作、状态和错误提示容易识别。
