# Asset Pivot Editor Agent Notes

- Read the root `AGENTS.md` before editing.
- Keep scripts runnable inside Unreal Editor Python.
- Preserve the existing `pivot_set_buttom.py` filename for compatibility.
- Usage: `README.md`; requirements: `REQUIREMENTS.md`; visual guidance: `DESIGN.md`; logs: `agent-log/`.
